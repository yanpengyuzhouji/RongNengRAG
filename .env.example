# RAG 知识库 — 环境变量配置模板
# 复制为 .env 并填入实际值

# ===== LLM API Key (OpenAI 兼容服务) =====
# 本地 vLLM/Xinference 等通常不校验 key, 可留空
# 云服务 (OpenAI / 阿里云百炼 / DeepSeek 等) 填真实 key
LLM_API_KEY=

# 说明: config.yaml 中 llm.openai.api_key 是直接配置方式;
#        通过环境变量 LLM_API_KEY 配置则优先级更高
